# 群聊权限管理 · 现状审查与设计方案

> 日期：2026-08-16
> 作者：DeepSeek Harness（接手会话）
> 范围：群聊/私聊权限、成员权限、群配置、管理操作鉴权
> 配套阅读：`项目交接_DeepSeekHarness.md`（第 7 节权限判断顺序）、`使用手册.md`（3.7 群组权限 / 5 私密与权限）

---

## 0. 一句话结论

现有权限体系**骨架正确、覆盖了大部分场景，但存在 4 个越权漏洞、5 处功能未完工、2 处费用/隐私风险**。核心问题是：**没有"角色"概念、没有统一判定入口、管理操作（改分类名）完全无鉴权、群配置 enabled=false 语义与白名单冲突**。

建议按「补洞 → 统一 → 体验」三阶段修复，数据模型基本不动，主要改 `authz` 判定层。

---

## 1. 现状（代码级梳理）

### 1.1 权限数据

| 数据 | 位置 | 内容 |
|---|---|---|
| 群配置 | `group_configs` 表（groups.mjs） | 群开关 enabled、类型规则 type_rules（off/free/prefix+前缀词）、成员私聊开关 member_private_chat、群默认成员权限 default_member_rules |
| 成员权限 | `member_permissions` 表（permissions.mjs） | 按 `QQ+群` 为每个类型配 查/增/改/删 四个布尔 |
| 身份 | `identities` 表 | QQ → 名字（一对一，可换名） |
| 条目归属/私密 | `entries.owner`（名字）+ `entries.is_private` | 私密条目仅归属人私聊可见；secret 字段（password）脱敏 |
| 全局白名单 | `config.json` bot.allowedGroups / allowedUsers | 未配置群的群消息白名单；私聊白名单（空=不限制） |

### 1.2 判定链路（3 处入口）

```
main.mjs（消息入口）
 ├─ 群消息：有 group_configs 配置 → 跳过 allowedGroups 白名单
 │          enabled 时再做触发匹配（prefix/free）→ groupPolicy.allowedTypes
 ├─ 私聊：遍历已配置群 → getGroupMemberInfo 查角色
 │        若属某群且该群 member_private_chat=false 且非 owner → 拒绝
 └─ handleIncoming（chat.mjs）→ 命令 / AI 工具
     每个命令/工具内：isTypeAllowed(groupPolicy, type) → memberCan(qq, gid, type, action) → canAccessEntry（私密条目）
```

私密条目判定（entries.mjs）：`canAccessEntry` = 非私密直接放行；私密则要求 `私聊 + entries.owner === 当前身份名字`。

### 1.3 评估顺序（现状）

1. 群级 `off` 硬限制（任何成员配置不能覆盖开启）—— 文档第 7 节
2. 触发方式：prefix / free（仅非命令消息；命令消息跳过前缀，只受类型限制）
3. 成员权限 `member_permissions`；没有则群 `defaultMemberRules`；再没有则默认全量
4. 私密条目额外按归属人/私聊

---

## 2. 发现的问题清单（按严重度）

### A. 越权 / 安全漏洞（必须修）

**A1. `/分类名` 命令和 AI `rename_type` 工具完全无鉴权**
- `commands.mjs` `/分类名`：任何人、任何群都能全局改分类显示名（影响所有群+网页）。
- `ai.mjs` `rename_type`：同样无 `isTypeAllowed`/`memberCan` 检查。
- 影响：这是**全局管理操作**，等于群里任意成员可修改全局配置。

**A2. `/确认删` 不复查权限，且跨群可执行**
- `pendingDeletes` 只按 `user_id` 记 Set，不绑群。
- 在群 A `/删`（当时权限 OK）→ 到群 B 或私聊 `/确认删 <id>` 直接 `deleteEntry`，**无任何权限复查**（`/确认删` 分支里没有 isTypeAllowed / memberCan / canAccessEntry）。
- AI 的 pendingOps 同理：注册时查过权限，但确认执行（`executePendingOp`）不复查；10 分钟窗口内权限变化可被利用（低概率，但应补）。

**A3. 群配置 `enabled=false` 时行为反转，可误放行**
- `main.mjs` 白名单判断：`!groupCfg`（**只要存在配置记录就跳过白名单**，不看 enabled）。
- 而 `enabled=false` 时 groupPolicy 保持 null → `isTypeAllowed(null) = true` → **全量自由**。
- 结果：一个"停用"的群配置 = 该群绕过 allowedGroups 白名单 + 全类型自由。停用 ≠ 删除，语义和实际行为完全相反，配置时极易踩坑。

**A4. 私聊成员拦截 fail-open + 不豁免管理员**
- `main.mjs` 私聊过滤：`getGroupMemberInfo` 返回 null（机器人掉线/接口失败/非群成员）时**不拦截**（fail-open）。
- 只豁免 `owner`，**群管理（admin）不豁免**。
- 每次私聊消息都遍历所有群配置并发起一次 OneBot 网络调用，无缓存。

### B. 功能未完工（交接文档已承认）

**B1. 群主/管理员没有成员级 CRUD 豁免**
- 文档第 7 节明确："成员级 CRUD 权限目前没有自动群主豁免，未配置则按群默认"。
- 后果：群配置了 `defaultMemberRules` 只读后，群主在群里也不能增/改/删，无法维护内容。

**B2. 私聊与群权限完全解耦（语义未定义）**
- `memberCan` 在 `gid` 为空时直接 `return true` → 群 A 关闭某类型，成员**私聊**仍可查/建该类型（非私密条目）。
- 这是产品决策问题：私聊=个人空间全量？还是受群约束？当前无文档说明。

**B3. 换身份名导致失去条目访问权**
- `entries.owner` 存**名字**；`/我是 新名字` 只更新 identities 表，不迁移旧条目。
- 改名后，旧条目（含私密）归属旧名字，本人无法再访问。身份是"QQ↔名字"一对一的，理应随身份迁移归属。

**B4. 网页管理端缺删除功能**
- API 有 `DELETE /api/groups/:id`、`DELETE /api/permissions/:qq/:gid`，但 `web/app.js` 的群组/成员权限页只有添加和编辑，**没有删除按钮**。

**B5. 群内没有任何"管理命令"体系**
- 唯一管理命令 `/分类名` 无鉴权（A1）；群主无法在群里查看/管理群配置。

### C. 体验 / 费用风险

**C1. free 模式（含未配置群默认）任何闲聊都触发 AI，有费用风险**
- 测试群 1103784008 未配置 = 默认全量 free，群友随便聊天（"今天天气不错"）也会调 DeepSeek 并计费。
- 需要"触发门槛"机制或明确文档。

**C2. 前缀限制可被命令绕过**
- prefix 模式群里，`/查 妖兽` 不带前缀也能用（命令跳过触发匹配，只受类型限制）。
- 可能是故意的（命令更明确），但文档 3.7 写的是"必须发前缀才会触发"，需要二选一：命令也要求前缀，或文档明确"命令不受前缀约束"。

### D. 一致性 / 隐私

**D1. 提醒发送不区分私密归属**
- `reminders.mjs` 用 `getDueNow()` 全量（含私密条目），全部私聊发给 `notify.qqUserId`。
- 单人场景无碍，但若未来 `notify.qqUserId` 不是条目归属人，私密提醒会泄露。应支持按归属人过滤。

**D2. `/权限` 在未配置群显示与事实不符**
- 未配置群（受 allowedGroups 白名单限制、甚至不在白名单内）`/权限` 仍显示"所有类型全量"，与群实际可用性不符。

---

## 3. 目标权限模型设计

### 3.1 核心原则

1. **单一判定入口**：所有权限判定收敛到一个 `authz.checkPerm(ctx, {type, action, entry?})`，返回 `{allowed, reason}`。杜绝散落判断（现在 commands.mjs / ai.mjs / entries.mjs 各查各的）。
2. **角色分层**：`owner`（群主）> `admin`（群管理）> `member`（群成员）> `stranger`（陌生人/私聊）。
3. **动作集扩展**：`read / create / update / delete`（成员级 CRUD）+ `manage`（管理动作：改分类名、配群权限、配成员权限）。
4. **群级 off 对普通成员是硬限制**；群主/管理员可豁免（见 3.4 决策点 P1）。
5. **权限判定与费用触发分离**：权限管"能不能做"，触发门槛管"什么话值得调 AI"。

### 3.2 数据模型（增量，不破坏现有表）

```sql
-- group_configs 增列（可选，见 P2）
ALTER TABLE group_configs ADD COLUMN admin_qq_ids TEXT NOT NULL DEFAULT '[]';  -- 群管理员白名单（JSON 数组）

-- 新表：全局管理员（可选，管理命令的兜底授权）
CREATE TABLE IF NOT EXISTS admins (
  qq_id TEXT PRIMARY KEY,
  note  TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL
);
```

- 角色来源：OneBot `get_group_member_info.role`（owner/admin/member），**带 60s 内存缓存**，失败时 fail-closed（按 member 处理并记日志）。
- 管理授权优先级：全局 admins 表 > 群 admin_qq_ids > 群内 owner/admin 角色 > 网页（网页仍是单用户全权）。

### 3.3 统一判定函数（新文件 `server/src/authz.mjs`）

```js
// 返回 { allowed: boolean, reason: string }
export function checkPerm(ctx, { type, action, entry = null }) {
  const { user_id, group_id, message_type, groupPolicy, groupCfg } = ctx;

  // 0) 私密条目：仅归属人私聊（现有 canAccessEntry 语义，保持不变）
  if (entry?.isPrivate) {
    if (message_type === 'group') return deny('私密条目仅归属人私聊可见');
    if (entry.owner !== identityName(user_id)) return deny('私密条目仅归属人本人可访问');
  }

  // 1) 管理动作：只对 管理员/群主/群管理 放行
  if (action === 'manage') {
    if (isAdmin(user_id) || isGroupRole(user_id, group_id, ['owner', 'admin'])) return allow();
    return deny('该操作需要群主/管理员权限');
  }

  // 2) 群级类型开关（off 硬限制）——普通成员不可覆盖
  if (action !== 'read' && groupPolicy?.allowedTypes && !groupPolicy.allowedTypes.includes(type)) {
    if (!isGroupRole(user_id, group_id, ['owner', 'admin'])) return deny('本群未开启该类型');
    // 群主/管理员可豁免（P1 决策）
  }
  if (groupPolicy?.mode === 'prefix' && !messageIsCommand && !matchedPrefix) { ... } // C2 决策

  // 3) 成员权限 → 群默认 → 默认全量（现有 memberCan 回退链，包一层角色豁免）
  if (isGroupRole(user_id, group_id, ['owner', 'admin'])) return allow();  // B1 修复
  const allowed = memberCan(user_id, group_id, type, action);
  return allowed ? allow() : deny('你在本群没有该类型的权限');
}
```

> 关键点：**角色豁免插在"群级 off"和"成员权限"之间**；私密条目的归属人检查永远在最前（私密性不因角色豁免而解除——即使是群主也不能看别人的私密条目）。

### 3.4 需要拍板的决策点

| # | 决策点 | 推荐默认 | 理由 |
|---|---|---|---|
| P1 | 群主/管理员是否豁免"群级 off 类型"？ | **豁免** | 群主有权管理自己的群；off 主要限制普通成员。若担心群主误开，可加"off 连群主也禁"的群配置项 |
| P2 | 群管理员白名单放哪？ | **group_configs.admin_qq_ids + 全局 admins 表** | 群配置只管本群，全局 admins 兜底（如未来有跨群管理员） |
| P3 | 私聊是否受群权限约束？ | **不受**（私聊=个人空间，仅受 member_private_chat 总开关约束） | 保持现状语义，文档写清；避免误伤家人私聊。未来可加"私聊也按身份限类型"的配置 |
| P4 | `/分类名`、rename_type 允许谁？ | **群主/管理员 + 全局 admins + 网页** | 管理操作，普通成员禁止 |
| P5 | free 群闲聊触发 AI 的门槛？ | **新增 `bot.groupAITrigger`：`any`（现状）/ `mention`（仅@机器人）/ `question`（含问句关键词）**，默认 `any` 但文档警告费用；群配置可覆盖 | 避免直接改行为吓到用户；把选择权给用户 |
| P6 | 前缀限制是否也约束命令？ | **命令仍不受前缀约束**（只受类型限制），文档写明 | 命令是显式意图，体验更好；改动会破坏现有习惯 |
| P7 | `enabled=false` 的语义？ | **= 不启用该群特殊规则，回退到"未配置群"逻辑（受 allowedGroups 白名单）** | 与"删除配置"行为一致，语义直观；修复 A3 |

### 3.4b 决策记录（2026-08-16 已与用户确认）

- **P1 → 群主不豁免**：群主/群管理也严格按成员权限执行，与普通成员完全一致（与现有文档第 7 节一致）。→ **Phase 2 的 B1"群主/管理员 CRUD 豁免"取消，不实现**。
- **P5 → 默认不变，加可配置项**：Phase 1 已在 `config.example.json` 加 `bot.groupAITrigger`（默认 `any`，行为与现状完全一致），`mention`/`question` 门槛留待 Phase 3 实现。
- **P4 落地方式**：管理员 = `config.bot.adminQqIds`（白名单）；为空时回退到 `notify.qqUserId`（配置者本人）。网页仍为单用户全权。
- **P7 已落地**：`main.mjs` 白名单判断改为"已配置且启用"才算配置群；`enabled=false` 的群回到白名单约束。
- **本次实施范围**：Phase 1（A1–A4 + B4 + P5/P7 配置），Phase 2/3 留待后续。

### 3.5 权限评估总顺序（修复后）

```
私密条目归属检查（任何角色不可豁免）
  → 管理动作检查（manage：全局 admins / 群 owner+admin）
  → 群级 off 硬限制（普通成员；owner/admin 按 P1 豁免）
  → 触发方式（prefix/free；命令按 P6）
  → 角色豁免（owner/admin 对 CRUD 默认放行，B1）
  → 成员权限 member_permissions → 群 defaultMemberRules → 默认全量
```

---

## 4. 分阶段实施计划

### Phase 1 · 安全补洞（改动小，风险低，建议先做）

1. **A1**：`/分类名` + AI `rename_type` 增加 `manage` 权限检查（先按 P4 默认：群主/管理员/全局 admins）。
2. **A2**：`/确认删` 执行时复查 `canAccessEntry + isTypeAllowed + memberCan(delete)`，并校验 `pendingDeletes` 记录来源群与当前群一致（若跨群则拒绝）；`executePendingOp` 增加一次复查。
3. **A3**：`main.mjs` 白名单判断改为 `有配置且 enabled` 才跳过白名单；`enabled=false` 时按"未配置群"处理（P7）。
4. **A4**：私聊成员拦截改为 fail-closed（查询失败按拒绝处理并告警）；豁免扩展到 admin；角色结果 60s 内存缓存，避免每次网络查询。
5. **B4**：web 群组/成员权限页补删除按钮（调用现有 DELETE API）。

### Phase 2 · 统一判定 + 角色体系（结构性改动）

6. 新建 `server/src/authz.mjs`，把 commands.mjs / ai.mjs 里的散落检查全部替换为 `checkPerm`。
7. ~~**B1**：群主/管理员 CRUD 豁免（P1）。~~ **已取消**：用户确认 P1 = 群主不豁免，群主也严格按成员权限执行。
8. **B3**：换名迁移——`registerIdentity` 改名时，在同一事务里把 `entries.owner` 旧名更新为新名（防止误伤重名：只迁移"该 QQ 旧身份名"下的条目；重名场景给出警告）。
9. **D2**：`/权限` 在未配置群时显示"该群未配置，按默认规则"并提示白名单状态。
10. 文档同步：`使用手册.md` 3.7/5、`项目交接_DeepSeekHarness.md` 第 7 节。

### Phase 3 · 体验（可选）

11. **C1**：`bot.groupAITrigger` 触发门槛（P5），默认 `any`，文档警告费用；群配置可覆盖。
12. **C2/P6**：文档明确"命令不受前缀约束"。
13. **B5**：群内管理命令扩展（如 `/群设置` 查看本群配置——只读，群主可见）。
14. **D1**：提醒按归属人过滤（私密条目只发给归属人 QQ；`notify.qqUserId` 是归属人时行为不变）。

### Phase 4（未来，不动）

- 账号密码加密、附件权限、更多机器人适配——与本设计无关，保持现状。

---

## 5. 风险与注意事项

- **角色信息依赖 OneBot 在线**：NapCat 掉线时角色查询失败，fail-closed 会误伤群主/管理员；需在缓存里保留最近一次成功结果（TTL 内复用，过期后 fail-closed）。
- **换名迁移的重名风险**：A 改名为"妈妈"而 B 原本是"妈妈"——迁移前检查目标名是否已被他人占用，占用则拒绝改名或只迁移无冲突条目。
- **不要一次改完所有行为**：Phase 1 与现状行为几乎无冲突（只堵漏洞）；Phase 2 的豁免/统一会影响群主体验，需要测试群 1103784008 实测后再推广到 346049634。
- **测试方法**：`/api/chat` 模拟接口可构造任意 user_id/group_id/message_type，覆盖各角色场景；角色查询用 `scripts/test-member.mjs` 辅助。

---

## 6. 实施状态（2026-08-16 · Phase 1 已完成并本地验证）

| 项 | 状态 | 改动文件 |
|---|---|---|
| A1 `/分类名` + AI rename_type 鉴权 | ✅ | `permissions.mjs`（isAdminUser）、`commands.mjs`、`ai.mjs` |
| A2 `/确认删` 绑场景+复查 / executePendingOp 复查 | ✅ | `commands.mjs`、`ai.mjs` |
| A3 enabled=false 回到白名单语义 | ✅ | `main.mjs` |
| A4 私聊拦截 fail-closed + 角色缓存 + 只查受限群 | ✅ | `main.mjs`、`bot/onebot.mjs`（新增 getMemberRole） |
| B4 网页权限页删除按钮 | ✅ | `web/app.js` |
| P5 配置项骨架（adminQqIds / groupAITrigger，默认不变） | ✅ | `config.example.json` |
| 文档同步 | ✅ | `使用手册.md`、本文件 |

**追加修复（2026-08-16 · 用户反馈"群组/成员权限保存无效"）**：

- **根因**：`web/app.js` 生成权限复选框时把中文标签（查/增/改/删）写进了 `data-default-action` / `data-perm-action`，提交的 rules 键名是中文；后端 `saveGroupConfig` / `saveMemberPermissions` 只认英文键（read/create/update/delete），结果**任何勾选都被丢弃，永远存成「只读」（read:true，增改删 false）**。属于原有代码 bug，被"默认只读"假象掩盖。已改为英文键提交、中文仅作显示标签（`web/app.js`）。
- 顺带修复：`/查` 与 AI `search_knowledge` 在"有匹配但无查看权限"时明确提示"有 N 条匹配但没有查看权限"，不再伪装成"没找到"（`commands.mjs` / `ai.mjs`）。
- 顺带加固：所有 `/api/*` JSON 响应加 `Cache-Control: no-store`（`routes.mjs`），避免浏览器启发式缓存导致保存后页面显示旧数据。
- 群组/成员权限卡片增加细粒度摘要（可用类型及触发方式、各类型查增改删），保存后变化肉眼可见。
- 成员权限页：输入群号后只显示该群已开启的分类（`web/app.js`）；保存时未提交的（群里关闭的）类型保留原规则，不再被默认值覆盖（`permissions.mjs`）。
- AI 误判修复：群内"记录/查询丹药、丹方、妖兽、灵宠、境界等修仙内容"被误拒为"本群只能使用 minecraft_mod"——原因是 AI 不知道这些子类属于修仙模组。已在提示词注入类型覆盖说明（TYPE_GUIDE）、`create_entry`/`search_knowledge` 工具描述补充子类说明、`inferTypeFromText` 扩充关键词（`ai.mjs`）。
- 确认码修复：prefix 模式下用户回复确认码（「确认创建 61」或纯「61」）会被触发规则静默忽略 → 新增 `looksLikeConfirmation()` 放行（`groups.mjs`/`main.mjs`/`routes.mjs`）；确认执行后清空该会话 AI 记忆，防止下一条消息被误读为确认回复（`chat.mjs`）。
- AI 记录重复判断 + 多版本回答：`create_entry` 前用 `getByTitle`（`entries.mjs`）预检同名条目——内容相同拒绝重复，内容不同则提示已有版本及提供者、确认后新增为多版本；系统提示新增规则 15：查询同名/同主题多版本时逐一列出 id 和提供者，不挑一个、不合并、不编造（`ai.mjs`）。
- **重大 bug 修复（2026-08-16）**：AI 创建的条目字段（payload）一直被静默清空——`create_entry` 注册待确认操作时字段存在 `params.payload`，但 `executePendingOp` 执行时读的是 `p.fields`（undefined）。修复：执行时读 `p.payload`（`ai.mjs`）。此前所有 AI 创建的条目都没有类型专属字段（如 丹药的 kind/effect/recipe、提醒的 date/time）。
- 类型合并：删除 important_date（重要日期），并入 note（备忘提示，label 改为"备忘提示"📌）：note 新增 date/time/advance_days/repeat_yearly 字段并参与提醒；提醒时间**精确到分钟**（`明天8:30`/`周五晚上8点`/`下午3点`，`time` 字段 HH:mm）；旧 important_date 数据自动迁移为 note（`db.mjs`）；`time.mjs` 扩展日期解析（周X/下周X/X号 + 时间提取），`computeRemindAt` 复用宽松解析；AI 规则 16 + 工具层兜底：note 无日期但消息含时间意图时强制 AI 补填（`ai.mjs`）。
- 相对时间提醒：`一分钟后`/`半小时后`/`10分钟后`/`2小时后`/`1小时30分钟后`/`一刻钟后` = 当前时间 + 偏移（`time.mjs` parseRelativeTime，支持中文数字；已实测"一分钟后"创建→到点真实触发私聊通知）。
- 提醒群发：条目记录创建时的群（`entries.source_group`，命令/AI 创建时写入），到点提醒**发回该群并 `[CQ:at]` @ 创建者**（身份名→QQ 反查，`identities.mjs` getQqByName）；私聊创建的提醒私发，或配置 `notify.groupId`（网页设置可改）统一发群（`reminders.mjs`）。
- 权限隐形：关闭的分类/无读权限 = **完全隐形**——搜索（AI search_knowledge 与 `/查`）、`/详情`、`get_entry`、改删操作被权限过滤时一律返回"没找到/条目不存在"，不透露"存在 N 条但无权限"；系统提示新增规则 17（`ai.mjs`/`commands.mjs`）。
- 位置字段修复：`create_entry` 工具原来**没有 location 参数**，AI 记录"XX放在哪"时位置信息被塞进 fields 后静默丢弃（item_location 只有 quantity 字段）。已加 `location` 参数 + 规则 18；重复判断比较包含 location；同名旧记录为空壳时自动注册"确认修改"补充进旧条目，避免新建重复（规则 19，`ai.mjs`）。
- 搜索类型词剥离：AI 把分类名/别名（"备忘""备忘提示"）当搜索关键词导致搜不到（如"看看我的备忘提示"→ query=备忘 → 空）。已在 `search_knowledge` 自动剥离类型词（TYPES 标签 + TYPE_ALIASES 别名 + 类型 key），并新增规则 20（`ai.mjs`）。
- 列表展示优化：规则 21——AI 列出多条条目时必须带内容要点/位置/提醒时间，不自行概括成"待办事项"等空洞标签（`ai.mjs`）。
- 备忘内容原话保存：规则 22 + 工具兜底——note 的 content 填用户原话（原封不动，如"明天12点记得提醒我冲电费"），AI 留空会被强制补填（`ai.mjs`）。

**注意**：现有 `server/config.json` 无需改动（`adminQqIds` 为空时自动回退 `notify.qqUserId`，即配置者本人仍可 `/分类名`；`groupAITrigger` 默认 `any` 与现状一致）。

**遗留（后续 Phase）**：Phase 2（authz 统一入口、换名迁移、/权限 提示）、Phase 3（groupAITrigger 的 mention/question 实现、群内管理命令、提醒归属过滤）。
