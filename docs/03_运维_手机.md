# docs/03 · 手机服务器运维（含 NapCat 部署）

> 手机运维 / NapCat 会话专用。**本文件自包含**：新会话只读这一份 + `00` 即可干活。
> 手机：红米 Note 9 Pro 5G，Linux Deploy Debian 12 arm64 容器，IP **192.168.31.208**。
> 更新日期：2026-08-16 深夜

## 1. 当前服务状态

| 服务 | 地址 | 状态 |
|---|---|---|
| my-wiki | http://192.168.31.208:8000 | ✅ 运行中（bot 已连 NapCat） |
| DSH | — | ❌ 已移除（只在电脑本地 127.0.0.1:3080） |
| NapCat QQ 机器人 | http://192.168.31.208:6099/webui | ✅ 运行中（QQ 3020150374 已登录，OneBot WS 4955 已连 wiki） |

## 2. 唯一入口：Linux Deploy 网页终端

- 手机保持 Linux Deploy app 运行（httpd 守护模式）
- 每次操作前先访问 `http://192.168.31.208:5080/cgi-bin/terminal` **触发 5081 端口的一次性 WebSocket 监听**，再用电脑脚本连
- 电脑辅助脚本（在 `C:\ComfyUI\`）：
  - `rawterm.ps1`：WebSocket 终端客户端。先 GET 终端页触发 5081，再 `-B64 <base64命令> -Port 5081`
  - `runcmd.ps1`：自动完成"触发+执行"两步（`-B64 <base64命令>`）
  - `httpserver.ps1 -Root C:\mc\rom -Port 8001`：PC 文件服务器（手机侧 curl/wget 拉文件用）
- **命令帧上限约 125 字节**：长命令/含引号命令会截断 → 一律写成脚本文件传到容器再执行（文件放 `C:\mc\rom\`，手机 `curl -o /data/local/mnt/tmp/x.sh http://192.168.31.180:8001/x.sh`）
- 提权/进容器：
  - 主机 root：`su -c "命令"`
  - 容器内：`su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin; 命令'"`
  - 注意：**chroot 内 PATH 必须显式 export**，否则找不到命令
- 长任务后台跑：`nohup 命令 >/日志 2>&1 &` 防终端断开被杀
- 传文件：PC `httpserver.ps1 -Port 8001` + 手机 `/system/bin/curl -o /data/local/mnt/tmp/xx http://192.168.31.180:8001/xx`

## 3. git 同步（开发流程）

1. 电脑改完测试 → `cd C:\my-wiki; git add -A; git commit -m "..."; git push origin main`
2. 手机：`su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin; cd /srv/wiki && git pull origin main'"`
3. 若 server/src 有改动需重启 wiki（见第 5 节）
4. `server/config.json`、`server/data/` 不随 git（.gitignore），各机器本地保留

### ⚠️ DNS 固定（勿删）
```
222.211.12.75 mirrors.aliyun.com
20.205.243.166 github.com
```
DNS 间歇失败；git/apt 失败先确认 /etc/hosts 这两行。

## 4. NapCat 部署（✅ 已完成 2026-08-16）

### 4.1 架构结论
NapCat v4 在 Linux 寄生在官方 QQ 客户端进程内（napcat.cjs 钩 process.dlopen，等 wrapper.node 加载后调 NCoreInitFramework）。注入：`LD_PRELOAD=/lib/arm64/libnapiloader.so` + `NAPCAT_EXTERNAL_SCRIPT_PATH=/opt/napcat/nativeLoader.cjs`，跑 `qq --no-sandbox`（Xvfb 虚拟显示）。参考官方 `NapNeko/NapCat.Docker.Framework`（支持 arm64，NapCat 4.18.19 配 QQ 3.2.30-50969）。

### 4.2 已完成
- ✅ `/opt/napcat`：NapCat.Framework v4.18.19 已解压（自带 linux-arm64 natives），nativeLoader.cjs 就位
- ✅ QQ deb + libnapiloader.so 已传到 `/root/napcat-install/`（q.deb 202MB / l.so 22.7MB；电脑备份在 `C:\mc\rom\`）
- ✅ libnapiloader.so 已复制到 `/lib/arm64/libnapiloader.so`
- ✅ **apt 源已修复（2026-08-16，见 4.3）**：apt-get update 正常，xvfb 等包可查可装
- ✅ **QQ 重装 + 依赖全装齐（2026-08-16）**：linuxqq 3.2.30-50969 已装；xvfb/dbus/libgtk-3-0/libnss3/libgbm1/libasound2/libatspi2.0-0/libnotify4/xdg-utils/libsecret-1-0/fonts-wqy-zenhei 全部安装成功（USTC ~4MB/s）
- ✅ **NapCat 启动 + 登录（2026-08-16）**：`/tmp/napcat_run.sh`（电脑 `C:\mc\rom\napcat_run.sh`）→ Xvfb :1 + LD_PRELOAD 注入成功；QQ 3020150374 扫码登录成功；WebUi token `1005309dfeaf`
- ✅ **wiki 对接（2026-08-16）**：手机 config.json bot.enabled=true、token=`SQeSFx59hT2YsfyR`（= NapCat onebot11 配置）、restartCommand=`sh /srv/wiki/scripts/restart-napcat.sh`、qrcodePath=/opt/napcat/cache/qrcode.png；wiki 日志 `[bot] 已连接 OneBot WS 127.0.0.1:4955`
- ✅ **验证通过（2026-08-16）**：测试群发 `/帮助` 正常回复 → **NapCat 部署完成**；电脑端 NapCat 停用（按决策：只需手机一个 QQ）
- ⚠️ 待办：NapCat 二维码 png 未落盘（/opt/napcat/cache/ 空，Framework 模式只出 URL），已登录不影响；未来重登需处理

### 4.3 apt 源（✅ 已修复 2026-08-16 深夜，勿再动）
- **现象**：curl 连 aliyun/ustc 都 200，但 apt 报 `socket (13: Permission denied)`（aliyun）或 `Could not resolve`（ustc，DNS 走 UDP socket 也被拒）
- **根因**：Android 系统**限制非 root UID 创建 socket**；apt 下载子进程默认以 `_apt`(uid 42) 运行 → socket 被拒（python `setuid(100)` 复现 FAIL，root 则 OK）
- **修复（已持久化）**：`/etc/apt/apt.conf.d/90android-sandbox` 写入 `APT::Sandbox::User "root";`
- **源**：USTC `deb http://mirrors.ustc.edu.cn/debian bookworm main contrib non-free`（/etc/hosts 固定 `202.141.160.110 mirrors.ustc.edu.cn`）；速度 ~2.2MB/s
- 旧 hosts 行 `222.211.12.75 mirrors.aliyun.com` 保留无妨（aliyun 也可用）；tuna http 403 不用
- apt 相关脚本备份在 `C:\mc\rom\`（apt-fix.sh 等旧脚本是修复前产物，可忽略）

### 4.4 部署步骤（2026-08-16 全部完成 ✅）
1. ✅ 修 apt 源（2026-08-16 完成，见 4.3）
2. ✅ 重装 QQ：`dpkg -i /root/napcat-install/q.deb && apt-get -f install -y`
3. ✅ 装依赖：`apt-get install -y xvfb dbus libgtk-3-0 libnss3 libgbm1 libasound2 libatspi2.0-0 libnotify4 xdg-utils libsecret-1-0 fonts-wqy-zenhei`
4. ✅ 启动：传 `C:\mc\rom\napcat_run.sh` 到容器执行（Xvfb :1 → qq --no-sandbox + LD_PRELOAD + NAPCAT_EXTERNAL_SCRIPT_PATH）
5. ✅ 扫码登录（二维码在 WebUi：http://192.168.31.208:6099/webui?token=1005309dfeaf）
6. ✅ 配 wiki：手机 `/srv/wiki/server/config.json` 设 `bot.enabled=true`、`token=SQeSFx59hT2YsfyR`（与 NapCat onebot11_3020150374.json 对齐）、`restartCommand=sh /srv/wiki/scripts/restart-napcat.sh`、`qrcodePath=/opt/napcat/cache/qrcode.png`；重启 wiki（`[bot] 已连接 OneBot WS`）
7. ✅ 验证：QQ 群发 `/帮助` 正常回复；电脑端 NapCat 已停用

### 4.5 已知配置
- 手机 wiki config.json：bot.enabled=true、kind=onebot11、host=127.0.0.1、port=4955、token=`SQeSFx59hT2YsfyR`、selfId=3020150374、allowedGroups=[1103784008]、restartCommand=`sh /srv/wiki/scripts/restart-napcat.sh`、qrcodePath=/opt/napcat/cache/qrcode.png（本地文件不随 git）
- 手机 NapCat OneBot WS：`/opt/napcat/config/onebot11_3020150374.json`（websocketServers 端口 4955、token SQeSFx59hT2YsfyR、messagePostFormat=array）
- NapCat WebUi：http://192.168.31.208:6099/webui?token=1005309dfeaf（token 在 qq.log 里）
- 电脑 config.json bot 段有完整示例（token/selfId/allowedGroups）
- 测试群 1103784008；用户 QQ 495538306；机器人 QQ 3020150374

## 5. 服务重启速查

```sh
# 进容器
su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin:/opt/node/bin; ...'"

# wiki 重启
pkill -f 'main[.]mjs'
cd /srv/wiki/server && nohup env TZ=Asia/Shanghai /opt/node/bin/node src/main.mjs > /srv/wiki/wiki.out.log 2>&1 &

# 健康检查
curl -s http://127.0.0.1:8000/api/health
```

## 6. 手机同步待办（✅ 已完成 2026-08-16，wiki 开发会话交接）

**背景**：电脑端已推送 `87063cc`（feat: 全量知识库 + 未配置群禁用机器人 + 知识库类型强制指令触发）。手机已同步。

**完成情况**：
1. ✅ **git pull**：github 443 不通 → 用镜像成功：`git pull https://ghfast.top/https://github.com/xiaosong724/mywiki.git main`（手机 HEAD 到 `76dbfd4`）
2. ✅ **重启 wiki**（`/tmp/restart_wiki.sh`），`[bot] 已连接 OneBot WS 127.0.0.1:4955`
3. ✅ **删 1103784008 群配置**：`node:sqlite` 删 `group_configs` 该行（剩余 346049634/876859661 均 enabled=0，与电脑一致）；新代码下未配置/停用群机器人完全不响应（已验证 `/api/chat` 返回 `{"reply":""}`）
4. ✅ **config.json 加 knowledge 段**：`"knowledge": {"minecraft_mod": {"mainFile": "data/knowledge/minecraft_mod/main.md", "dynamicFile": "data/knowledge/minecraft_mod/dynamic.md"}}`
5. ✅ **传 md**：电脑 tar 打包（`tar -czf`）→ 手机 curl 下载 → python3 解压（**容器无 gzip，GNU tar 解 .tgz 报错，用 python3 tarfile 解**）；注意 tar 包内路径根是 `minecraft_mod/`，解到 `/srv/wiki/server/data/knowledge/` 下
6. ✅ **验证**：health OK；`/api/knowledge/minecraft_mod` 返回完整 main/dynamic/versions；未配置群与已删群 chat 均 reply 空

**经验**：手机传文件优先用 tar 包（curl 下载命令短）；容器解压用 python3（无 gzip）；`server/data/` 沙箱外写不了时走 `.tmp`（已 gitignore）。

> DSH 手机端已删除，勿再尝试启动。浏览器安全上下文补丁（randomUUID）只在 DSH 需要，手机 wiki 不受影响。
