# docs/03 · 手机服务器运维（含 NapCat 部署）

> 手机运维 / NapCat 会话专用。**本文件自包含**：新会话只读这一份 + `00` 即可干活。
> 手机：红米 Note 9 Pro 5G，Linux Deploy Debian 12 arm64 容器，IP **192.168.31.208**。
> 更新日期：2026-08-16 晚

## 1. 当前服务状态

| 服务 | 地址 | 状态 |
|---|---|---|
| my-wiki | http://192.168.31.208:8000 | ✅ 运行中 |
| DSH | — | ❌ 已移除（只在电脑本地 127.0.0.1:3080） |
| NapCat QQ 机器人 | — | ⏳ 部署中（见第 4 节） |

## 2. 唯一入口：Linux Deploy 网页终端

- 手机保持 Linux Deploy app 运行（httpd 守护模式）
- 每次操作前先访问 `http://192.168.31.208:5080/cgi-bin/terminal` **触发 5081 端口的一次性 WebSocket 监听**，再用电脑脚本连
- 电脑辅助脚本（在 `C:\ComfyUI\`）：
  - `rawterm.ps1`：WebSocket 终端客户端。先 GET 终端页触发 5081，再 `-B64 <base64命令> -Port 5081`
  - `runcmd.ps1`：自动完成"触发+执行"两步（`-B64 <base64命令>`）
  - `httpserver.ps1 -Root C:\mc\rom -Port 8001`：PC 文件服务器（手机侧 curl/wget 拉文件用）
- **命令帧上限约 125 字节**：长命令/含引号命令会截断 → 一律写成脚本文件传到容器再执行（文件放 `C:\mc\rom\`，手机 `curl -o /data/local/mnt/tmp/x.sh http://192.168.31.180:8001/x.sh`）
- 提权/进容器：
  - 主机 root：`su -c "命令"`
  - 容器内：`su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin; 命令'"`
  - 注意：**chroot 内 PATH 必须显式 export**，否则找不到命令
- 长任务后台跑：`nohup 命令 >/日志 2>&1 &` 防终端断开被杀
- 传文件：PC `httpserver.ps1 -Port 8001` + 手机 `/system/bin/curl -o /data/local/mnt/tmp/xx http://192.168.31.180:8001/xx`

## 3. git 同步（开发流程）

1. 电脑改完测试 → `cd C:\my-wiki; git add -A; git commit -m "..."; git push origin main`
2. 手机：`su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin; cd /srv/wiki && git pull origin main'"`
3. 若 server/src 有改动需重启 wiki（见第 5 节）
4. `server/config.json`、`server/data/` 不随 git（.gitignore），各机器本地保留

### ⚠️ DNS 固定（勿删）
```
222.211.12.75 mirrors.aliyun.com
20.205.243.166 github.com
```
DNS 间歇失败；git/apt 失败先确认 /etc/hosts 这两行。

## 4. NapCat 部署（进行中）

### 4.1 架构结论
NapCat v4 在 Linux 寄生在官方 QQ 客户端进程内（napcat.cjs 钩 process.dlopen，等 wrapper.node 加载后调 NCoreInitFramework）。注入：`LD_PRELOAD=/lib/arm64/libnapiloader.so` + `NAPCAT_EXTERNAL_SCRIPT_PATH=/opt/napcat/nativeLoader.cjs`，跑 `qq --no-sandbox`（Xvfb 虚拟显示）。参考官方 `NapNeko/NapCat.Docker.Framework`（支持 arm64，NapCat 4.18.19 配 QQ 3.2.30-50969）。

### 4.2 已完成
- ✅ `/opt/napcat`：NapCat.Framework v4.18.19 已解压（自带 linux-arm64 natives），nativeLoader.cjs 就位
- ✅ QQ deb + libnapiloader.so 已传到 `/root/napcat-install/`（q.deb 202MB / l.so 22.7MB；电脑备份在 `C:\mc\rom\`）
- ✅ libnapiloader.so 已复制到 `/lib/arm64/libnapiloader.so`
- ⚠️ QQ 装过又被 `apt-get -f install` 卸载（依赖源失败导致），**需重装**

### 4.3 ⛔ 当前卡点：apt 源
- `/etc/apt/sources.list` 是 `deb.debian.org`（DNS 解析不了）；换 aliyun 报 `Release 文件不存在`（/etc/hosts 固定 IP `222.211.12.75` 可能失效）
- **下一步先测**：`curl -I http://mirrors.aliyun.com/debian/dists/bookworm/Release`
- 不通就换源：清华 `deb http://mirrors.tuna.tsinghua.edu.cn/debian bookworm main contrib non-free`（加进 /etc/hosts）或 USTC，再 `apt-get update`

### 4.4 继续步骤（按序）
1. 修 apt 源（见上）→ `apt-get update -qq`
2. 重装 QQ：`dpkg -i /root/napcat-install/q.deb && apt-get -f install -y`
3. 装依赖：`apt-get install -y xvfb dbus libgtk-3-0 libnss3 libgbm1 libasound2 libatspi2.0-0 libnotify4 xdg-utils libsecret-1-0 fonts-wqy-zenhei`
4. 启动：传 `C:\mc\rom\napcat_run.sh` 到容器执行（Xvfb :1 → qq --no-sandbox + LD_PRELOAD + NAPCAT_EXTERNAL_SCRIPT_PATH）
5. 扫码登录（二维码在 `/opt/napcat/cache/qrcode.png`）
6. 配 wiki：手机 `/srv/wiki/server/config.json` 设 `bot.enabled=true`、`host=127.0.0.1`、`port=4955`、`qrcodePath=/opt/napcat/cache/qrcode.png`、加 `bot.restartCommand`（NapCat 版重启脚本）；重启 wiki
7. 验证：QQ 群发 `/帮助` 看回复；电脑端 NapCat 可停用

### 4.5 已知配置
- 手机 wiki config.json 当前：bot.enabled=false、无 restartCommand、无 qrcodePath（本地文件不随 git）
- 电脑 config.json bot 段有完整示例（token/selfId/allowedGroups）
- 测试群 1103784008；用户 QQ 495538306

## 5. 服务重启速查

```sh
# 进容器
su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin:/opt/node/bin; ...'"

# wiki 重启
pkill -f 'main[.]mjs'
cd /srv/wiki/server && nohup env TZ=Asia/Shanghai /opt/node/bin/node src/main.mjs > /srv/wiki/wiki.out.log 2>&1 &

# 健康检查
curl -s http://127.0.0.1:8000/api/health
```

> DSH 手机端已删除，勿再尝试启动。浏览器安全上下文补丁（randomUUID）只在 DSH 需要，手机 wiki 不受影响。
