# 我的知识库 · 使用手册

> 版本：v0.5 · 最后更新：2026-08-16
> 本文只记录**已经设定好并测试通过**的功能；开发中的功能在文末「功能状态」里单独列出。
> 同内容已存入知识库（type=help），网页顶部「📖 使用帮助」页和 AI 问答都会从数据库读取；改功能时三个地方一起更新。

## 1. 系统是什么

自建个人知识库，两个入口：

- **网页**（http://localhost:8000）：总体查看、搜索、管理
- **QQ 机器人**：记录、修改、查询、提醒（测试群 1103784008，机器人由 NapCat 提供）

数据存 SQLite 单文件（`server/data/`），换服务器 = 复制整个 data 目录。

## 2. 身份系统（重要）

群里有家人时，机器人靠「身份」区分谁是谁。

| 操作 | 方式 |
|---|---|
| 绑定身份 | 发 `/我是 名字`，或直接说「我的名字是XX」 |
| 查看已登记 | `/身份` |
| 换名字 | 再发一次 `/我是 新名字` |
| 首次说话 | 未登记的人发自然语言，机器人会先引导登记 |

已登记：**SY = QQ 495538306**。

绑定后：

- 机器人用名字称呼你
- 说「我的XX」按你名下的条目查找
- 新记的东西默认归属到你名下

## 3. QQ 命令（不走 AI，零费用）

| 命令 | 作用 | 示例 |
|---|---|---|
| `/帮助` | 查看命令 | `/帮助` |
| `/记 类型 标题 字段=值…` | 记录 | `/记 食物 牛奶 过期=明天` |
| `/查 关键词` | 搜索 | `/查 钥匙` |
| `/详情 id` | 看详情 | `/详情 a1b2c3d4` |
| `/改 id 字段=值…` | 修改 | `/改 a1b2c3d4 数量=2` |
| `/删 id` + `/确认删 id` | 两步删除 | `/删 a1b2c3d4` |
| `/提醒 [N天]` | 未来提醒 | `/提醒 7` |
| `/过期` | 已到期食品 | `/过期` |
| `/今天` | 今天到期 | `/今天` |
| `/密码 id` | 私聊查看密码 | `/密码 a1b2c3d4` |
| `/我是 名字` | 绑定身份 | `/我是 SY` |
| `/身份` | 已登记列表 | `/身份` |
| `/权限` | 查看本群分类权限 | `/权限` |
| `/分类名 类型 新名称` | 改分类显示名（需管理员） | `/分类名 模组 修仙模组` |
| `/清空` | 重置对话记忆 | `/清空` |

类型别名：模组/知识/账号/琐事/食物/位置/生日/旅行。

字段别名：`位置`、`备注`、`标签`、`过期`、`日期`、`时间`、`生日`、`农历`、`提前` 等，如
`/记 生日 妈妈生日 生日=3月8日 农历=是 提前=3`，或 `/记 琐事 交房租 日期=明天 时间=8:30`。

### 3.5 标签（Tag）

标签是跨类型的自由关键词，可多个，用来补充分类、方便搜索，卡片上会显示。

- 命令：`/记 食物 牛奶 标签=冰箱,重要`
- 自然语言：「记一下厨房水龙头要修，标签是 重要、家里」
- 查询：「标签是「重要」的有哪些？」或 `/查 重要`（关键词也会匹配标签）
- 修改标签：「把这条的标签改成 家里」

### 3.6 Minecraft 模组 wiki

修仙 Addon 的开发知识用 `minecraft_mod` 类型管理，按 `kind` 区分：项目、药材、丹药、矿石、妖丹、妖兽、灵宠、境界、五行能力、指令、更新公告。

- 来源：从 `C:\mc\xiuxian_addon` 的 `manifest.json / constants.js / elements.js / 更新公告.md` 自动导入
- 玩法说明：`知识库_玩家版.md` 按章节导入为 `guide` 条目；问玩法/数值/概率时 AI 会优先检索 guide，回答「妖兽生成/炼丹/灵宠」等设定问题
- 导入命令：`node scripts\import-mc-mod.mjs`（重复运行是幂等更新，改源码后重跑同步）
- 清空某个 mod：`node scripts\reset-mc-mod.mjs xiuxian_addon`
- 问法示例：「回灵丹需要什么材料」「妖兽 12 阶后期攻击多少」「哪些矿石给灵石」「1.0.567 更新了什么」

### 3.7 群组权限

网页「⚙️ 群组权限」页可以给不同 QQ 群单独配置可用类型和触发方式：

- **关闭**：这个群不能使用该类型
- **自由触发**：群里直接问就能用
- **前缀触发**：必须发 `自定义前缀 问题` 才会触发，例如给 Minecraft 模组设置前缀 `wiki`，群里发「wiki 妖兽怎么生成」才会回答

未配置的群保持默认：所有类型自由触发。命令类（`/帮助`、`/我是`、`/身份`、`/清空`）不受限制；`/记`、`/查` 等命令会按群配置限制类型。

**停用（⛔）的群配置**：等同于"未配置"，回到默认规则，并受 `bot.allowedGroups` 白名单约束（不在白名单的群机器人直接忽略）。想彻底取消某个群的配置，直接删除该群配置即可。

群配置里还可以关掉「允许群成员私聊机器人」：关闭后，该群成员不能私聊机器人，但群主始终可以。

**管理操作**：`/分类名`（改分类显示名）是全局管理操作，仅管理员可用——`config.json` 的 `bot.adminQqIds` 列表；该列表为空时默认为 `notify.qqUserId`（配置者本人）。

## 4. 自然语言（走 DeepSeek，有费用）

不用记命令，直接说就行：

- 查询：「牛奶放哪里了？」「我的生日还有多久」
- 记录：「帮我记一下周三去买洗衣液」「我的生日是1995农历7月24，每年提醒我」
- 修改：「把豆腐的过期日期改成8月20」
- 删除：「删掉重复的生日记录」（会走确认码，见第 7 节）

每次自然语言调用后，机器人会**私聊**你一条费用报告：本次费用、今日累计、钱包余额（官方接口实时查询）。

**变更都要两位确认码**：自然语言里的记录、修改、删除、分类改名，AI 只会先给一个两位确认码，你回复「确认XX 码」或直接回两位码才会真正执行（确认后自动清空该话题的 AI 记忆，防止下一条消息被当成确认回复）。

**重复判断与多版本**：AI 记录前会检查知识库里是否已有同名条目——

- 内容完全相同：提示「已有相同条目，无需重复记录」，不会重复添加
- 内容不同：确认信息里会列出已有版本及提供者（如「已有 1 条同名补天丹：#e1b11992（提供：FG）」），确认后新增为**另一个版本**（多版本并存，各自保留提供者）

查询时若同名/同主题有多个版本，AI 会**逐一列出每个版本的 id 和提供者**并给出内容要点，不会只挑一个回答，也不会自行合并或编造。

## 5. 私密与权限

- 条目可以标记**私密**（网页表单勾选「私密」，或命令/AI 记录时说明）
- **账号类条目默认自动私密**
- 私密条目的规则：
  - 群里永远搜不到、看不到
  - 只能**归属人本人在私聊**里查/改/删
  - 别人私聊也看不到
- 密码类内容：AI 只提示存在，不吐明文；想查看用 `/密码 id`（仅私聊可用）
- 成员权限：网页「👥 成员权限」可以给某个 QQ 在指定群设置每个分类的查/增/改/删权限；未配置的成员默认拥有群里开启类型的全部权限
- **关闭 = 完全隐形**：群里关闭的分类、或成员没有"查"权限的分类，搜索/详情/问答一律显示"没找到"，不会透露"存在 N 条但没权限"之类的信息

## 6. 提醒

- 系统每 30 秒检查一次到期提醒
- **到点发回创建时的群并 @ 创建者**（`[CQ:at]`）；私聊创建的提醒私发给 `notify.qqUserId`，或在网页设置里配「提醒群」（`notify.groupId`）统一发群
- **备忘提示（琐事）可精确到分钟**：`日期=明天8:30`、`周五晚上8点`、或分开填 `日期=明天 时间=08:30`；也支持相对时间 `一分钟后`、`半小时后`、`2小时后`（当前时间直接加偏移）；不填时间默认早上 9 点
- 提前提醒：食物过期、备忘提示、生日都支持 `提前=N天`（备忘默认 0=到点提醒）
- 每年重复：生日（含农历）、备忘提示勾选「每年重复」后自动顺延
- 机器人离线时提醒不丢：上线后自动补发
- 查询：`/提醒`、`/过期`、`/今天`，或直接问「最近有什么提醒」

### 农历生日

生日类型支持农历：字段填 `生日=月-日` + `农历=是`（可选 `出生年份`）。
系统每年自动换算成阳历并提前提醒。例：1995 农历 7/24 → 2026 年阳历 09-05，提前 3 天（09-02）提醒。

## 7. 删除安全

删除一律二次确认：

- **AI 删除**：机器人给出 `确认删除 短码`，你回复「确认删除 短码」才执行
- **命令删除**：`/删 id` 后再 `/确认删 id`（确认时必须回到发起删除时的同一个群/私聊，且权限未变才执行）
- 确认码是**两位数**（如 `确认删除 81`）；AI 回复异常时也可用 `/确认删 两位数` 兜底
- 私密条目只能归属人本人在私聊里删

## 8. 网页使用

- **仪表盘**：已到期 + 未来 7 天提醒
- **类型页**：8 种类型各自入口（模组/知识/账号/琐事/食物/位置/生日/旅行）
- **搜索**：顶部全局搜索
- **新建/编辑**：右下角 ＋ 按钮，表单按类型自动生成；可填归属人、勾选私密
- **详情**：点卡片进入，可编辑/删除
- **日志**：顶部「日志」页查看所有操作记录（含机器人操作）
- **备份**：顶部「💾 备份」页手动创建/删除备份，恢复数据用
- **设置**：顶部「🛠️ 设置」页修改 AI 上限/模型/提醒等配置，也可以改分类名称（如 Minecraft 模组 → 修仙模组）
- **成员权限**：顶部「👥 成员权限」配置成员在指定群的分类增删改查权限

## 9. 配置文件（server/config.json）

| 字段 | 说明 | 当前值 |
|---|---|---|
| port / host | 服务端口/监听地址 | 8000 / 0.0.0.0 |
| token | 设置后 /api 需带 X-Auth-Token | 空（仅局域网） |
| scheduler.intervalSeconds | 提醒扫描间隔 | 30 |
| backup.intervalHours / retention | 自动备份间隔（小时）/保留份数 | 24 / 10 |
| notify.qqUserId | 提醒私聊对象 | 495538306 |
| bot.enabled | 是否连 QQ 机器人 | true |
| bot.host/port/token | OneBot WS 连接 | 127.0.0.1:4955 |
| bot.allowedGroups | 允许的群白名单 | [1103784008] |
| bot.allowedUsers | 允许的私聊白名单（空=不限制） | [] |
| bot.adminQqIds | 管理员 QQ 列表（管理操作如改分类名；空=回退到 notify.qqUserId） | [] |
| bot.groupAITrigger | 群消息触发 AI 的门槛，预留（当前恒为 any=所有非命令消息都触发，后续版本实现 mention/question） | any |
| ai.enabled / apiKey / model | DeepSeek 开关/密钥/模型 | true / 已填 / deepseek-chat |
| ai.costNotifyQQ | 费用报告私聊对象 | 495538306 |
| ai.pricing | 单价（元/百万 tokens），官方调价就改这里 | deepseek-chat 0.02/1/2 |

## 10. 数据与备份

- 全部数据在 `server/data/`：`knowledge.db`（条目+身份+日志）、`usage.json`（AI 用量）
- 备份位置：`server/data/backups/backup-<时间戳>/`，里面是 SQLite `VACUUM INTO` 生成的**一致快照**
- 手动备份：网页「💾 备份」页、`POST /api/backups`、或命令行 `node scripts/backup-now.mjs`
- 自动备份：服务启动 60 秒后先做一次，之后每 `backup.intervalHours` 小时一次，保留最近 `backup.retention` 份
- 恢复：停服务 → 用备份里的 `knowledge.db` 覆盖 `server/data/knowledge.db` → 删除 `knowledge.db-wal/-shm` → 重启
- 换服务器/换机器人：数据不动，只改配置

## 11. 功能状态

### ✅ 已测试可用

- 8 种类型增删改查 + 搜索
- 网页：仪表盘 / 类型页 / 详情 / 日志 / 动态表单
- QQ 命令 14 条 + 自然语言问答/记录/修改/删除
- 身份绑定（首次引导、/我是、AI 自动登记）
- 私密权限（群隐藏、私聊本人可见、密码脱敏）
- 农历生日换算 + 每年提醒
- 到期提醒 + 机器人离线自动补发
- AI 费用 + 余额报告私聊
- 删除确认码 + /确认删 兜底
- 自动备份 + 网页/接口/脚本手动备份
- Minecraft 模组 wiki（修仙 Addon 自动导入 + kind 分类）

### ⏳ 开发中 / 待办

- 手机服务器（Debian）部署与开机自启
- 账号密码加密存储（目前是明文存在本地库，仅限局域网）
- 附件/图片上传（表结构已留，功能未接）
- 前端升级 Vue
- 更多消息渠道（微信等）
