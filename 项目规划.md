# 个人知识库 Wiki — 项目规划

> 日期：2026-08-15
> 部署环境：红米 Note 9 Pro 5G + Linux Deploy Debian 12 (arm64)，6GB 内存，局域网部署

## 1. 目标

一个自托管的个人知识库系统，两个入口：

- **网页**：总体查看、搜索、管理（增删改查）
- **QQ 机器人（NapCat）**：消息提醒、记录、修改、询问

内容类型：我的世界模组功能设定、电脑文件知识、账号信息、生活琐事、重要日期/备忘录、食品过期、物品存放位置、家人生日、旅游记录。

核心要求：**扩展性强、换 QQ 机器人零成本交接、DeepSeek 调用省钱方便、能长期跑在手机服务器上。**

## 2. 总体架构（分层解耦）

```
┌──────────────┐   ┌──────────────────┐
│  网页前端      │   │   QQ 机器人端      │
│  Vue 3 SPA   │   │   NapCat (OneBot11)│
└──────┬───────┘   └────────┬─────────┘
       │  HTTP/REST         │ WebSocket/HTTP
┌──────┴────────────────────┴─────────┐
│             核心服务 (FastAPI)         │
│  ├ 类型注册中心（每种 wiki 的定义）     │
│  ├ 条目服务（CRUD + 检索）             │
│  ├ 提醒调度器（APScheduler）          │
│  ├ 通知适配器（QQ / 网页 / 未来其他）   │
│  ├ AI 网关（DeepSeek + 工具调用）      │
│  └ 数据仓库（SQLite + 附件文件）       │
└────────────────┬────────────────────┘
                 │
        data/knowledge.db + data/files/
```

**核心原则：一切功能都走核心 API，机器人只是"翻译官"。**

换 QQ 机器人 = 换一个适配器；数据、提醒、AI、网页全部不动。

## 3. 数据存储：SQLite（单文件）

选择 SQLite 的理由：

- 单文件数据库，**备份 = 复制一个文件**，交接 = 复制整个 `data/` 目录
- 手机服务器零额外服务，arm64 + Python 内置支持，无需再跑 MySQL/PostgreSQL
- WAL 模式支持读写并发，个人使用完全足够
- 数据量级（个人知识库，几万条以内）远未到 SQLite 瓶颈

### 3.1 核心表结构

`entries`（条目主表，所有类型共用）：

| 字段 | 说明 |
|---|---|
| id | UUID 主键 |
| type | 类型标识，如 `minecraft_mod` / `food_expiry` |
| title | 标题 |
| content | 正文文本 |
| payload | JSON，类型专属字段（灵活扩展的关键） |
| tags | 标签 |
| location | 存放位置（物品类用） |
| remind_at | 下次提醒时间（可空） |
| recurrence | 重复规则：none / daily / weekly / yearly |
| notified_at | 上次已通知时间（防重复提醒） |
| done | 完成/归档标记 |
| created_at / updated_at | 时间戳 |

`attachments`（附件表）：条目关联图片/文件，文件本体存 `data/files/`。

`event_log`（操作日志）：所有增删改留痕，机器人误操作可恢复。

### 3.2 为什么所有类型共用一个表

- **新增一种 wiki 类型不需要改表结构**，只要在"类型注册中心"里加一个类型定义
- 提醒、过期、生日这些"时间驱动"的条目统一用 `remind_at + recurrence` 字段，调度器只扫一张表
- JSON payload 存类型专属字段，用 Pydantic 校验，兼顾灵活和安全

### 3.3 类型注册中心（扩展性的核心）

每种类型在代码里声明：名称、图标、字段列表（含表单校验）、是否参与提醒、检索权重。

规划的内置类型：

| type | 典型字段 |
|---|---|
| minecraft_mod | 模组名、版本、功能设定、配置文件路径、备注 |
| computer_knowledge | 主题、分类、内容、相关文件 |
| account | 平台、账号、密码、密保、备注（当前仅本地局域网，密码明文存本地库，暂不加密） |
| note | 琐事内容、重要程度 |
| important_date | 日期、是否每年重复、提前 N 天提醒 |
| food_expiry | 食物名、存放位置、过期日期、提前 N 天提醒 |
| item_location | 物品名、位置、数量、照片 |
| birthday | 姓名、关系、公历/农历、提前 N 天提醒 |
| travel | 地点、时间、照片、感想 |

## 4. 提醒系统

- 调度器每 30~60 秒扫一次：`remind_at <= now 且 未通知`
- 触发后走"通知适配器"发送（QQ 私聊/群聊 + 网页横幅）
- 发送后写 `notified_at`，防重复
- 循环提醒（生日、纪念日 = yearly）：通知后自动计算下一次 `remind_at`
- 提前提醒：`important_date` / `birthday` 存"提前 N 天"，由调度器在到期前 N 天触发

## 5. QQ 机器人：NapCat 适配器模式

NapCat 实现 OneBot 11 协议。核心服务与它之间只有一个适配器：

```python
class BotAdapter(Protocol):
    async def send_message(self, target, text): ...   # 发消息
    async def on_message(self, handler): ...          # 收消息
```

- 现在：`NapCatAdapter` 实现 OneBot11（WS 或 HTTP 连接）
- 未来：换任何机器人 = 新写一个 Adapter + 改配置，数据/核心逻辑零改动
- NapCat 跑在哪里都可以（电脑或服务器），只要核心服务能连到它的 OneBot 端口

消息处理流程：

1. **明确命令直接解析，不调 AI（零成本）**：如 `/记 食物 牛奶 过期7.20`、`/查 位置 钥匙`、`/提醒 明天 交话费`
2. **自然语言走 DeepSeek 工具调用**：AI 把"记录/修改/删除/询问"翻译成工具调用，核心 API 执行，AI 再总结回复
3. **危险操作二次确认**：删除条目、解密密码必须回一句确认，防止 AI 误操作

## 6. DeepSeek 集成：最省成本方案

核心思路：**只让 AI 做"翻译"，不让 AI 记数据；先检索后生成；能不用 AI 就不用 AI。**

### 6.1 检索（RAG）

- 第一层：检索（本地、免费、毫秒级），搜出 top 5~10 条相关条目。中文短词先走 LIKE（对 2 字查询更准），FTS5 表保留给后续长文/向量优化
- 把检索结果 + 工具定义喂给 DeepSeek，让它基于真实数据回答，**不靠模型记忆**
- 可选升级：本地 bge 小模型做 embedding 增强召回（手机也能跑，前期不需要）

### 6.2 工具调用（function calling）

DeepSeek 工具列表：

- `search_knowledge(query)`：全文检索
- `get_entry(id)`：读详情
- `create_entry(type, title, fields...)`：记录
- `update_entry(id, fields...)`：修改
- `delete_entry(id)`：删除（需二次确认）
- `list_due_reminders()`：查询待提醒
- `decrypt_secret(id)`：解密账号密码（需二次确认，且回复敏感字段前提示）

### 6.3 省钱要点

- 明确命令（`/记`、`/查` 等）完全不调用 AI
- 会话上下文只保留最近几轮，不无限累积
- 检索结果只截取相关字段喂给模型，不整篇塞入
- `max_tokens` 控制回复长度，回复尽量简短
- **默认用 DeepSeek API 最便宜够用的档位**，复杂任务才用更强的模型
- 密码等敏感字段对 AI **默认脱敏**，AI 拿到的只是"某平台有账号记录"，不泄漏明文

## 7. 网页管理界面

- Vue 3 + Vite 构建成静态文件，由 FastAPI 一起托管（少一个端口和进程）
- 页面：
  - 仪表盘：今日提醒、即将过期、最近添加
  - 类型视图：每种类型独立入口（列表/卡片/详情）
  - 全局搜索（同一套 FTS5）
  - 动态表单：按类型注册中心自动生成添加/编辑表单
  - 设置页：DeepSeek key、机器人配置、备份下载
- 单用户登录（密码 + 会话 token）
- 建议只在局域网用；要远程访问就用 Tailscale，不暴露公网

## 8. 备份与交接

- `data/` 目录 = 全部数据：`knowledge.db` + `files/` + `usage.json`
- 备份机制（已实现）：网页「💾 备份」页 + `/api/backups` 接口 + `scripts/backup-now.mjs`
  - 用 SQLite `VACUUM INTO` 生成一致快照，备份目录 `data/backups/backup-<时间戳>/`
  - 自动备份默认每 24 小时一次，保留最近 10 份（`config.backup` 可调）
- **换 QQ 机器人流程**：停旧适配器 → 改配置（机器人连接信息、QQ 号、群绑定）→ 启新适配器。知识库、提醒、AI 零影响
- **换服务器流程**：打包复制整个 `data/` 目录即可
- 部署和启动步骤见 `部署交接_手机服务器.md`，手机连接方法见 `手机服务器连接交接.md`

## 9. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | Node.js（>= 22.5）+ 内置 `node:http` | 零依赖，单进程，arm64/Windows 都原生支持 |
| 数据库 | SQLite（WAL）+ 内置 `node:sqlite` | 单文件、零运维；内置 FTS5 可用 |
| 调度 | 内置 setInterval + 到期扫描 | 提醒 + 自动备份，均简单可靠 |
| HTTP/WS | 内置 fetch / WebSocket | 调 DeepSeek API + 连 NapCat OneBot WS |
| 前端 | 原生 HTML/JS（当前）+ Vue 3（后续升级） | 先跑通测试，类型注册中心驱动动态表单 |
| 部署 | `node src/main.mjs` + 启动脚本 | 手机重启后一键拉起 |

内存预估：核心服务 < 80MB，前端无常驻内存，6GB 手机完全够用。

> **为什么从 Python/FastAPI 换成 Node**：本地测试机没有 Python（只有 Node 26），而 Node 26 自带 SQLite/WebSocket/fetch，整个后端零依赖；NapCat 插件本身就是 JS，全项目一种语言；手机 Debian 上部署也只需一个 node 二进制。架构设计不变（类型注册中心、适配器、API-first 全部保留）。

## 10. 实施阶段

- **Phase 0**：项目骨架、配置、数据库、类型注册中心
- **Phase 1**：核心 API + 网页（浏览/搜索/增删改）
- **Phase 2**：提醒调度 + 通知（网页横幅 + 日志）
- **Phase 3**：NapCat 适配器 + DeepSeek RAG + 工具调用 + 二次确认
- **Phase 4**：加密（账号字段）、备份、部署交接文档
- **Phase 5（可选）**：本地 embedding、更多机器人适配、更多类型

### 当前进度（2026-08-15 本地测试）

- ✅ Phase 0 完成：零依赖 Node 后端、SQLite 建库、9 种类型注册中心
- ✅ Phase 1 大部分完成：REST API（增删改查/搜索/到期提醒）+ 单文件网页管理界面
- ✅ Phase 2 骨架：30s 调度器、到期提醒、QQ 私聊推送（机器人在线时）
- ✅ Phase 3 前置：OneBot11 WS 适配器、命令模式（/记 /查 /改 /删 /提醒 /过期）
- ✅ Phase 3 联调：NapCat WS 已连接、测试群消息发送成功、DeepSeek 自然语言工具调用实测（搜索/记录/删除确认）
- ✅ 费用统计：每次 AI 调用后按 tokens 计费 + 官方余额接口查询，私聊推送给指定 QQ
- ✅ 身份识别：首次说话强制绑定（/我是 或 自然语言）、多人身份、条目归属人
- ✅ 私密权限：条目私密标记、账号类默认私密、群里不可见、仅归属人私聊可查改删
- ✅ 农历生日：lunar-javascript 纯 JS 库换算，每年自动转阳历提醒
- ✅ 删除安全：服务端确认码机制（防 AI 误删/假删）、搜索结果带 id 支持"删除第N条"、/确认删 兜底、/清空 重置记忆
- ✅ 使用手册入库：type=help 存 9 节手册（脚本 scripts/store-manual.mjs 可重复执行）、网页「📖 使用帮助」页从数据库渲染、AI 可检索回答"怎么用"
- ✅ 搜索优化：多关键词自动拆分（AND 匹配）、私聊可见性与归属过滤分离（修复公共条目被误过滤）
- ✅ 标签功能：AI 创建/修改支持 tags、按标签查询、标签过滤 SQL 修复
- ✅ 备份：`/api/backups` + 网页备份页 + 自动备份调度 + `scripts/backup-now.mjs`
- ✅ Minecraft 模组 wiki：`minecraft_mod` 类型扩展 `kind` 子类型，`scripts/import-mc-mod.mjs` 从 `C:\mc\xiuxian_addon` 源码/文档导入（药材/丹方/矿石/妖丹/妖兽/灵宠/境界/指令/更新公告/玩家指南）
- ✅ 群组权限：网页「⚙️ 群组权限」配置不同群可用类型与触发方式（关闭/自由/前缀），消息链路按群限制搜索/记录/修改/删除；支持控制群成员私聊权限（群主除外）
- ✅ 网页设置页：可在线修改 AI 上限/模型/提醒等运行配置；分类名称可自定义（类型显示名重命名）
- ✅ AI 变更确认码：自然语言记录/修改/删除/分类改名统一走两位确认码，回复确认码才执行
- ✅ 成员权限：网页「👥 成员权限」按群+成员+分类配置查/增/改/删；群内 `/权限` 查看本人详细权限
- ⏳ Phase 3 待做：用户在 QQ 测试群 1103784008 实测机器人回复
- ⏳ Phase 4 待做：部署到手机服务器实测（账号字段加密已按用户决定暂缓，备份和部署交接文档已完成）

本地测试方法见 `本地测试指南.md`。
已测试功能与使用说明见 `使用手册.md`（随开发同步更新）。

## 11. 开发用 Flash 还是 Pro

结论：**混合使用，Pro 打底，Flash 迭代。**

- 项目是"中等复杂度、大量集成胶水"：CRUD、数据库、网页、OneBot 适配、AI 工具调用
- **Pro**：总体架构、类型注册中心、提醒调度、AI 工具调用协议——这些"想清楚一次少返工"的部分
- **Flash**：日常 CRUD 页面、适配器样板代码、测试、小改动——快速试错刷量
- 不想来回切的话：全程 Pro 最稳；这个项目规模不大，全程 Pro 成本也可控
- **运行时（机器人实际调 DeepSeek）用官方 API 最便宜的档位做默认**，和开发用哪个模型是两回事

## 12. 需要确认的风险点

1. **NapCat 跑在哪**：NapCat 需要 QQ 桌面端环境，可能更适合跑在电脑上而不是手机容器里。需要在 Phase 3 验证，好在架构上完全不影响（适配器只管连 OneBot 端口）
2. **手机重启**：容器需要手动启动或配置开机自启，提醒会暂时失效
3. **公网暴露**：默认只走局域网；远程用 Tailscale 最省事
4. **微信接入已暂缓**：原计划的 Gewechat（AstrBot 同款）已停止维护、仓库技术归档，不再按旧教程部署；等有合规稳定的微信入口再评估，当前专注 QQ + 本地
