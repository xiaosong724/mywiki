# 手机连接交接（新会话用）

## 手机基本信息

- 红米 Note 9 Pro 5G（M2007J17C / gauguin），MIUI 13.0.15（V13.0.15.0.SJSCNXM），Android 12
- 已解锁 BL + Magisk root
- WiFi IP：**192.168.31.208**（IP 可能变化，连不上时先确认手机当前 IP）
- 存储剩余约 98.7GB；运行内存 6GB（可用约 5.3GB）
- 手机上有 Linux Deploy + Debian 12 (bookworm, arm64) 容器（BDS 已删除，容器保留待用）

## 唯一可用入口：Linux Deploy 网页终端（SSH 未安装）

1. 手机保持 Linux Deploy app 运行（httpd 守护模式开着；容器不用启动也能开终端）
2. 浏览器访问：**http://192.168.31.208:5080/cgi-bin/terminal**
3. 得到 Android 主机 shell（用户 u0_a212）
4. 验证：输入 `su -c id`，返回 `uid=0(root)` 即成功

## 提权 / 进容器

- 执行 root 命令：`su -c "命令"`
- 进入 Debian 容器执行：
  `su -c "chroot /data/local/mnt /bin/sh -c 'PATH=/usr/bin:/bin:/usr/sbin:/sbin; 命令'"`
  - 容器实际目录：`/data/local/debian`（Linux Deploy 目录模式，bind 挂载到 `/data/local/mnt`）
- 挂载/启动容器：`su -c "sh /data/user/0/ru.meefik.linuxdeploy/files/cli.sh mount"`
- 容器 CLI（等同 app 的启动/停止/进 shell）：
  `su -c "sh /data/user/0/ru.meefik.linuxdeploy/files/cli.sh shell 命令"`

## 网页终端注意事项（重要）

- **命令单帧上限约 125 字节**，超长命令会被丢弃；长操作拆成短命令，或写成脚本文件传过去执行
- 终端会话偶发"落在容器内"，主机路径会报错——重试几次即可
- 长任务用 `su -c "nohup 命令 >/日志路径 2>&1 &"` 防止终端断开被杀死
- 每次访问终端页才会在 **5081 端口**（HTTP 端口+1）临时起一个 WebSocket 监听，一次性使用

## 电脑端辅助脚本（可选）

- WebSocket 终端客户端：`C:\ComfyUI\rawterm.ps1`
  - 先 GET `http://192.168.31.208:5080/cgi-bin/terminal` 触发 5081 监听
  - 再执行：`powershell -File C:\ComfyUI\rawterm.ps1 -B64 <base64编码的命令> -Port 5081`
- 局域网传文件：文件放 `C:\mc\rom`，运行 `C:\ComfyUI\httpserver.ps1`（端口 8000），
  手机端 `su -c "wget -O /目标路径 http://<电脑IP>:8000/文件名"`

## 其它

- Linux Deploy 配置用户：fangou / 密码 wufan.123（SSH 尚未安装，暂时用不上）
- 手机重启后：打开 Linux Deploy → 点「启动」恢复容器；网页终端需要 app 在运行
