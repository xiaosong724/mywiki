# 部署交接（Windows 本地 → 手机 Debian）

> 目标：把 `C:\my-wiki` 的整套系统迁到红米 Note 9 Pro 5G 上的 Linux Deploy Debian 12 (arm64)，长期运行。
> 本文件是部署交接说明；连接手机的方法见 `手机服务器连接交接.md`。

## 0. 一句结论

**核心服务是 Node.js 零依赖单进程，数据是 `server/data/` 一个目录。**

- 程序：`server/` + `web/`
- 数据：`server/data/knowledge.db` + 附件（以后有）+ `usage.json`
- 配置：`server/config.json`（含 key，不上传到公共仓库）
- 网页：`web/` 静态文件，由核心服务同端口托管

迁移 = 复制程序 + 复制数据 + 装 Node + 改配置 + 启动。

## 1. 手机容器环境准备

1. 进入 Linux Deploy 的 Debian 12 容器（方法见 `手机服务器连接交接.md`）。
2. 确认架构和已装软件：

```sh
uname -m          # 应为 aarch64
which node || echo 'no node'
```

3. 安装 Node.js。**不要用 apt 默认的老版本**（Debian 12 默认 Node 18 缺少 `node:sqlite`）。

推荐用官方预编译 arm64 包（22.5 以上，建议 22 LTS 或 26）：

```sh
# 在电脑上下载 Linux arm64 的 node 二进制后，通过 http 局域网传到手机，
# 解压到 /opt/node，然后：
export PATH=/opt/node/bin:$PATH
node -v
```

> 因为项目用了 `node:sqlite`，必须是 Node >= 22.5。装好后确认：
>
> ```sh
> node -e "require('node:sqlite')"
> ```

4. 确认 `lunar-javascript` 依赖：项目唯一的 npm 依赖在 `server/node_modules`。
   迁移时可以整目录复制（Windows 和 arm64 都是纯 JS，依赖通用），
   或到 `server/` 下执行 `npm install`。

## 2. 复制程序和数据

手机终端单帧命令有长度限制（约 125 字节），不要直接在网页终端里粘贴大文件；
建议在电脑端用 `手机服务器连接交接.md` 里的 `httpserver.ps1` 方式传文件，或先打成压缩包。

需要复制的内容：

```text
server/       后端（含 src、config.json、data、node_modules）
web/          网页静态文件
scripts/      可选，备份脚本
```

最小运行集就是 `server/` 和 `web/`。

## 3. 配置手机端

编辑 `server/config.json`（注意它不入库）：

- `host`：局域网用 `0.0.0.0`，这样电脑浏览器和 QQ 都能访问
- `port`：默认 `8000`
- `bot.*`：指向手机端 NapCat 的 OneBot 地址（NapCat 和核心服务是否在同一台机器看实际部署）
- `ai.*`：DeepSeek key 已填则保留；不要在公开仓库提交
- `notify.qqUserId`：提醒私聊接收者（默认用户 QQ 495538306）

## 4. 启动核心服务

```sh
cd /path/to/server
node src/main.mjs
```

验证：

```sh
curl http://127.0.0.1:8000/api/health
```

返回 `ok: true` 即成功。

长期运行（防止网页终端断开后进程被杀）：

```sh
cd /path/to/server
nohup node src/main.mjs > /path/to/wiki.log 2>&1 &
```

## 5. 开机自启（可选）

Debian 容器重启后手动 `node src/main.mjs`，或写一个 systemd/init 脚本。
最简单的是在容器启动脚本里加一行 `nohup node ... &`，具体按 Linux Deploy 的启动方式调整。

## 6. 备份与恢复

备份机制：

- 网页「💾 备份」页可以手动创建/删除备份
- `POST /api/backups` 手动备份；`GET /api/backups` 查看列表
- `config.backup.intervalHours`（默认 24 小时）自动备份，保留 `config.backup.retention`（默认 10）份
- `node scripts/backup-now.mjs` 命令行手动备份

备份位置：

```text
server/data/backups/backup-<时间戳>/
├── knowledge.db      # VACUUM INTO 生成的一致快照
├── usage.json        # AI 用量统计
└── ...               # 以后可能有附件等其它数据文件
```

恢复步骤：

1. 停掉核心服务
2. 把目标备份目录里的 `knowledge.db` 覆盖回 `server/data/knowledge.db`
3. 删除旧的 `knowledge.db-wal` / `knowledge.db-shm`（如有）
4. 把备份目录里的其它文件放回 `server/data/`
5. 重启核心服务

## 7. 换 QQ 机器人（数据零影响）

NapCat 换新机器人、换 QQ 号、换协议适配器时，只改 `server/config.json` 的 `bot` 段：

```json
"bot": {
  "enabled": true,
  "kind": "onebot11",
  "host": "127.0.0.1",
  "port": 4955,
  "token": "...",
  "selfId": "...",
  "allowedGroups": ["1103784008"]
}
```

数据、提醒、AI、网页都不受影响。未来接微信等其它平台时，在 `server/src/bot/` 加一个适配器即可。

## 8. 当前状态备忘（2026-08-15）

- 本地已跑通：核心 CRUD、网页、QQ 命令、自然语言 AI、身份/权限、提醒、农历生日、删除确认码、费用统计、标签、备份
- 微信暂不接入（Gewechat 已停止维护，先专注 QQ + 本地）
- NapCat 本地测试配置：OneBot WS `127.0.0.1:4955`
