# 本地测试指南（Windows）

> 目标：在电脑上把「核心服务 + 网页 + NapCat 机器人」整个链路跑起来，验证后再迁到手机服务器。

## 1. 运行核心服务

```powershell
cd C:\my-wiki
node C:\my-wiki\server\src\main.mjs
```

（或运行 `scripts\start.ps1`）

- 网页管理界面：http://localhost:8000
- 首次启动自动建库；灌入演示数据（有数据则跳过）：

```powershell
node C:\my-wiki\server\src\seed.mjs
```

## 2. 配置文件

`server/config.json`（敏感项不入库，已 gitignore）：

| 字段 | 说明 |
|---|---|
| port / host | 服务端口与监听地址 |
| token | 设置后所有 /api 需带 `X-Auth-Token` 头 |
| scheduler.intervalSeconds | 提醒扫描间隔（默认 30s） |
| backup.intervalHours | 自动备份间隔小时数（默认 24，0 表示关闭） |
| backup.retention | 自动备份保留份数（默认 10） |
| notify.qqUserId | 提醒推送给哪个 QQ 号 |
| bot.* | OneBot WS 连接（已指向本机 NapCat 4955） |
| ai.* | DeepSeek 配置（默认关闭，填 apiKey 并 enabled=true 启用） |
| ai.costNotifyQQ | 每次 AI 调用后，费用+余额私聊发给哪个 QQ（默认 495538306） |
| ai.pricing | 单价（元/百万 tokens），按官方价格配置，改了官方价格就改这里 |
| notify.qqUserId | 到期提醒私聊发给哪个 QQ（默认 495538306） |

当前本地配置：

- 机器人只在测试群 **1103784008** 回复，其他群一律忽略（`bot.allowedGroups`）
- DeepSeek 已启用（`ai.enabled=true`，key 已填在 config.json，该文件不入库）
- 每次自然语言调用后：模型 `deepseek-chat`，本次费用按 tokens 实时计算，钱包余额走官方 `GET /user/balance` 接口查询，结果私聊发给 `ai.costNotifyQQ`

### 身份与权限（v0.2）

- 首次说话的人必须绑定身份：发 `/我是 名字`，或直接说「我的名字是XX」（AI 自动登记）
- `/身份` 查看已登记列表；SY 已绑定 QQ 495538306
- 条目有「归属人」和「私密」属性；账号类条目自动私密
- 私密条目：群里永远不显示；只能归属人本人在**私聊**里查/改/删；密码需 `/密码 id` 在私聊查看
- 农历生日：生日类型支持 `lunar=true`，每年自动换算成阳历提醒（如 1995 农历 7/24 → 2026-09-05）
- 删除安全：AI 删除必须走"确认码"——AI 会给出 `确认删除 <短码>`，回复它才执行；也可用 `/确认删 <短码>` 兜底；`/清空` 可重置对话记忆

## 3. 启动本机 NapCat

前置条件：本机 QQ（绿色版 `C:\game\qq`）已登录。

1. 右键管理员运行 `C:\my-wiki\NapCat.Shell\launcher.bat`
   - 提示 `provided QQ path is invalid` 时，参考 `工作交接_NapCat.md` 改脚本指定 QQPath 或补注册表
2. NapCat 起来后，核心服务日志会出现 `[bot] 已连接 OneBot WS 127.0.0.1:4955`
3. 给本地测试 QQ（3020150374）发消息测试：

```
/帮助
/查 钥匙
/记 位置 充电器 位置=床头柜
/提醒 7
```

测试群：**1103784008**。也可以直接发自然语言，如「牛奶放哪里了？」「帮我记一下周三去买洗衣液」。

发一条调试消息到指定群（不改服务也能用）：

```powershell
node scripts\test-send.mjs 1103784008 "测试消息"
```

## 4. 直接测后端（不启动 NapCat 也能测）

```powershell
# 健康检查
Invoke-RestMethod http://localhost:8000/api/health

# 模拟机器人消息
Invoke-RestMethod -Method Post -Uri http://localhost:8000/api/chat -ContentType 'application/json' `
  -Body (@{ text='/查 牛奶'; user_id='3020150374' } | ConvertTo-Json)
```

## 5. 备份

- 网页「💾 备份」页可手动创建/删除备份
- 接口：`GET /api/backups`、`POST /api/backups`、`DELETE /api/backups/<name>`
- 命令行手动备份：`node scripts\backup-now.mjs`
- 备份目录：`server\data\backups\backup-<时间戳>\`，含一致性的 `knowledge.db` 快照和 `usage.json`
- 自动备份：服务启动 60 秒后先做一次，之后按 `backup.intervalHours` 执行；按 `backup.retention` 清理旧份
- 恢复：停服务 → 用备份里的 `knowledge.db` 覆盖 `server\data\knowledge.db` → 删除 `knowledge.db-wal` / `knowledge.db-shm` → 重启

## 5.5 Minecraft 模组 wiki 导入

修仙 Addon 知识库用 `minecraft_mod` 类型 + `kind` 子类型管理，数据从真实源码/文档自动导入：

```powershell
# 从 C:\mc\xiuxian_addon 导入（默认路径）
node scripts\import-mc-mod.mjs

# 指定别的 addon 目录
node scripts\import-mc-mod.mjs C:\mc\xiuxian_addon

# 清空某个 mod 已导入的条目（按 mod_id）
node scripts\reset-mc-mod.mjs xiuxian_addon
```

导入内容：项目总览、15 药材、6 丹方、18 矿石、80 妖丹、15 妖兽、8 灵宠、12 境界、15 五行能力、12 指令、更新公告，以及 `知识库_玩家版.md` 按章节拆成的 `guide` 玩法说明。

重复导入是**幂等更新**：按 `mod_id+kind+key` 生成固定 ID，改完源码后重跑会更新旧条目，不会重复堆。

## 5.6 群组权限

网页「⚙️ 群组权限」页可以给不同 QQ 群单独配置可用类型和触发方式。

- 数据接口：`GET /api/groups`、`POST /api/groups`（创建/更新）、`DELETE /api/groups/<群号>`
- 三种触发模式：
  - `off`：该群不能使用这个类型
  - `free`：群里直接问即可
  - `prefix`：必须发「自定义前缀 问题」，例如 `wiki 妖兽怎么生成`
- 未配置的群保持默认：所有类型自由触发
- 命令 `/帮助`、`/我是`、`/身份`、`/清空` 不受限制；`/记`、`/查`、`/改`、`/删` 按群配置限制类型
- 群配置里可关闭「允许群成员私聊机器人」；关闭后该群成员私聊会被拒绝，群主始终允许（需 NapCat 在线以查询群成员角色）
- 网页「🛠️ 设置」里可以修改分类名称，例如把 `Minecraft 模组` 改成 `修仙模组`；数据接口 `GET/POST /api/type-labels`
- 自然语言记录/修改/删除/分类改名统一需要两位确认码，回复「确认XX 码」或直接回码才执行
- 网页「👥 成员权限」按群+成员+分类配置查/增/改/删；群内发 `/权限` 查看本人详细权限；未配置成员默认全量权限

## 6. 常见问题

- **机器人没连上**：NapCat 没启动，或 bot 段 host/port/token 与 `NapCat.Shell\config\onebot11_3020150374.json` 不一致
- **提醒没收到**：`notify.qqUserId` 填的是自己的 QQ；机器人必须在线（OneBot WS 已连接）
- **到期提醒会重试**：机器人离线时不标记已发送，上线后自动补发，不会丢
- **换 QQ 机器人**：改 `server/config.json` 的 bot 段即可，数据/提醒/AI 不受影响

## 6. 结构速览

```
C:\my-wiki\
├── server\             核心服务（Node 零依赖）
│   ├── config.json     运行配置（本地测试）
│   ├── data\           SQLite 数据库 + 附件 + backups\（备份=复制整个目录）
│   └── src\
│       ├── main.mjs    入口
│       ├── types.mjs   类型注册中心（加类型=改这里）
│       ├── entries.mjs 条目 CRUD/检索/提醒
│       ├── commands.mjs QQ 命令解析（零成本，不走 AI）
│       ├── ai.mjs      DeepSeek 工具调用网关
│       └── bot\onebot.mjs  OneBot11 适配器
├── web\                网页管理界面
├── NapCat.Shell\       本地 NapCat（QQ 机器人运行包）
├── scripts\start.ps1   启动脚本
└── 工作交接_NapCat.md   NapCat 经验参考（勿改）
```
