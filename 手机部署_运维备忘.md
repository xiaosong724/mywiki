# 手机部署 · 运维备忘（2026-08-16）

> 配套 `部署交接_手机服务器.md`。本文记录**实际部署完成状态**与日常运维方法。
> 手机：红米 Note 9 Pro 5G，Linux Deploy Debian 12 arm64 容器，IP **192.168.31.208**。

## 1. 当前运行的服务

| 服务 | 地址（局域网） | 状态 | 说明 |
|---|---|---|---|
| my-wiki 网页/API | http://192.168.31.208:8000 | ✅ 运行中 | 含 QQ 登录二维码/重启按钮、AI、权限、提醒、备份 |
| DeepSeek Harness (DSH) | — | ❌ 已移除 | **DSH 只在电脑本地跑**（127.0.0.1:3080），手机端已删干净（/opt/node/bin/dsh、node_modules/@deepseek-ai、/root/.dsh、dsh 日志），node/npm 保留给 wiki 用 |
| QQ 机器人 NapCat | — | ⏳ 待装 | NapCat.Framework v4.18.19 已解压到 `/opt/napcat`，**缺框架引导器**（需 BootWay/launcher 提供 QQ 会话），暂未启动 |

电脑端（192.168.31.180）同时保留：my-wiki（8000）、DSH（3080，本机 127.0.0.1 与局域网均可）、NapCat.Shell（QQ 机器人继续由电脑担任）。

**开发流程（2026-08-16 起）**：改动在电脑本地测试 → 用户确认后 `git push` → 手机 `/srv/wiki` `git pull` 同步。DSH 不再部署到手机。

## 2. 软件环境（容器内）

- Node：`/opt/node`（v22.23.2 arm64，含 npm）
- 编译工具：gcc 12.2 / g++ / make / python3（110 个 .deb 由 dpkg 本地安装）
- git：2.39.5
- 服务进程：wiki（node src/main.mjs）、dsh（dsh web）均用 `nohup ... &` 常驻

## 3. PC ↔ 手机 代码同步（git）

- 仓库：https://github.com/xiaosong724/mywiki（公开，SSH 推送）
- PC：`C:\my-wiki` 是 git 仓库，改完代码后：
  ```powershell
  cd C:\my-wiki
  git add -A && git commit -m "说明" && git push origin main
  ```
- 手机：`/srv/wiki` 是 git 仓库（工作树即运行目录），同步：
  ```sh
  cd /srv/wiki && git pull origin main
  # 若 server/src 或 web 有更新，重启 wiki：
  pkill -f 'main[.]mjs'; cd /srv/wiki/server && nohup env TZ=Asia/Shanghai /opt/node/bin/node src/main.mjs > /srv/wiki/wiki.out.log 2>&1 &
  ```
- `server/config.json`、`server/data/` 被 .gitignore 排除，**不随 git 同步**，各机器本地保留。

### ⚠️ 手机 DNS 固定（重要，已写入 /etc/hosts，勿删）

手机容器 DNS 解析**间歇性失败**（apt/git 都会挂）。已把常用域名固定到 /etc/hosts：

```
222.211.12.75 mirrors.aliyun.com
20.205.243.166 github.com
```

若同步失败，先确认这两行还在。apt 源为 `mirrors.aliyun.com`（bookworm main），`apt-get update` 偶发失败可重试。

## 4. 网页「重启 QQ/NapCat」功能

- 设置页「🤖 QQ 机器人登录」区块：显示 `qrcode.png`（NapCat cache）+「刷新二维码」+「重启 QQ/NapCat」按钮
- 重启命令在 `config.bot.restartCommand`（网页设置可改；电脑端已配置为 `powershell -File scripts\restart-napcat.ps1`，会杀 QQ+NapCat 并重启）
- 二维码路径：`config.bot.qrcodePath`（默认 `NapCat.Shell/cache/qrcode.png`；手机端 NapCat 部署后改为对应路径）

## 5. 手机 NapCat 部署进度（2026-08-16 晚，未完成）

### 5.1 架构结论（已研究清楚）

NapCat v4 在 Linux 上的官方做法（参照官方 `NapNeko/NapCat.Docker.Framework`，支持 **arm64**）：

- 装**官方 QQ Linux arm64 客户端**（`linuxqq_3.2.30-50969_arm64.deb`，来自 `qqdl.gtimg.cn/qqfile/QQNT/9.9.32/beta/fd40a3ec/`，202MB）→ 安装到 `/opt/QQ`，内含 `qq` 二进制（Electron 壳 + NTQQ 核心 `wrapper.node`）
- `napcat.cjs`（Framework 里那个）通过钩住 `process.dlopen`，等 QQ 加载 `wrapper.node` 时拿到 session/loginService，再调 `NCoreInitFramework` —— **所以 NapCat 必须寄生在 QQ 进程内跑**
- 注入方式：启动 QQ 时 `LD_PRELOAD=/lib/arm64/libnapiloader.so` + `NAPCAT_EXTERNAL_SCRIPT_PATH=/opt/napcat/nativeLoader.cjs`（`libnapiloader.so` 从 NapCat.Docker.Framework 仓库 `napi/arm64/` 下载，22.7MB）
- QQ 是图形程序 → 容器内需 **Xvfb** 虚拟显示（Dockerfile 里还有 fluxbox/x11vnc/noVNC，手机不需要，只装 Xvfb 即可）
- 登录后 NapCat 把二维码存到 `cache/qrcode.png`（`NapCatPathWrapper` 默认写到 napcat 目录的 `cache/`，即 `/opt/napcat/cache/qrcode.png`）

### 5.2 已完成

- ✅ `/opt/napcat` 已有 NapCat.Framework v4.18.19（自带 linux-arm64 natives：packet/pty/ffmpeg/napi2native），`nativeLoader.cjs` 就位
- ✅ QQ deb 已下载到电脑 `C:\mc\rom\linuxqq_3.2.30-50969_arm64.deb`（202MB）；`libnapiloader.so` 已下载到 `C:\mc\rom\libnapiloader.so`（22.7MB，MD5 `DA1F1A8D7DB2430A6A935D9CA631DA9D`）
- ✅ 两个文件已传到手机容器 `/root/napcat-install/`（q.deb / l.so，**已移出 /tmp 防丢失**）；`napcat_setup.sh`/`napcat_fix.sh` 也在那
- ✅ `dpkg -i q.deb` 装过 QQ 到 `/opt/QQ`（qq 二进制在），但**随后 `apt-get -f install` 因依赖源失败把 linuxqq 卸载了**，需重装
- ✅ `libnapiloader.so` 已复制到 `/lib/arm64/libnapiloader.so`

### 5.3 明天卡点（从这继续）

1. **apt 源挂了**：`/etc/apt/sources.list` 又变回 `deb.debian.org`（DNS 解析不了）；换 aliyun 后报 `Release 文件不存在`——`mirrors.aliyun.com` 当前 IP 可能失效（/etc/hosts 里固定的是 `222.211.12.75`）。**先测 `curl -I http://mirrors.aliyun.com/debian/dists/bookworm/Release`**，不通就换源（清华 `mirrors.tuna.tsinghua.edu.cn` 或 USTC），并更新 /etc/hosts
2. **重装 QQ**：`dpkg -i /root/napcat-install/q.deb` + `apt-get -f install -y`
3. **装 Xvfb + 依赖**：`apt-get install -y xvfb dbus libgtk-3-0 libnss3 libgbm1 libasound2 libatspi2.0-0 libnotify4 xdg-utils libsecret-1-0 fonts-wqy-zenhei`
4. **启动**：电脑端 `C:\mc\rom\napcat_run.sh`（Xvfb :1 → `LD_PRELOAD=/lib/arm64/libnapiloader.so NAPCAT_EXTERNAL_SCRIPT_PATH=/opt/napcat/nativeLoader.cjs qq --no-sandbox`）已写好，传到容器执行；扫码登录
5. **配 wiki**：手机 wiki `config.json` 设 `bot.enabled=true`、`host=127.0.0.1`、`port=4955`、`qrcodePath=/opt/napcat/cache/qrcode.png`，加 `bot.restartCommand`（NapCat 版重启脚本）；电脑端 NapCat 可停用

> 手机 wiki config.json 当前：`bot.enabled=false`、无 restartCommand、无 qrcodePath（本地文件，不随 git 同步）。
> 电脑端 config.json 有 bot 段完整示例（含 token/selfId/allowedGroups）。

## 5.5 浏览器安全上下文补丁（crypto.randomUUID）

- **现象**：`http://<局域网IP>:3080` 访问 DSH 时，"设置工作区"等报 `crypto.randomUUID is not a function`
- **原因**：`crypto.randomUUID` 只在安全上下文（HTTPS / localhost）可用；局域网 HTTP 访问时浏览器无此 API
- **修复**：在 `dsh-web-frontend/dist/index.html` 的 `<head>` 注入兜底补丁（用 `crypto.getRandomValues` 实现 UUID v4）
- **⚠️ 升级/重装 `@deepseek-ai/dsh` 后需重新注入**（补丁被覆盖）；注入方法：备份原 index.html，在 `<script type="module">` 前插入补丁脚本（见电脑端 `C:\Users\49553\AppData\Local\npm-cache\_npx\...\dsh-web-frontend\dist\index.html` 同款补丁）

## 6. 恢复/重启速查

```sh
# 进容器执行（手机网页终端）
su -c "chroot /data/local/mnt /bin/sh -c 'export PATH=/usr/bin:/bin:/usr/sbin:/sbin:/opt/node/bin; ...'"

# wiki
cd /srv/wiki/server && nohup env TZ=Asia/Shanghai node src/main.mjs > /srv/wiki/wiki.out.log 2>&1 &

# DSH
export DSH_HOME=/root/.dsh; cd /root && nohup dsh web > /root/dsh2.log 2>&1 &
```
